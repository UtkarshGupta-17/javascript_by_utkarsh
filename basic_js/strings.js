const newstring="utkarsh"
const repocount=50
//console.log(newstring+repocount); 
console.log(`hello my name is ${newstring} and my repocount  is ${repocount}`);// this is called string itnerpolation
const gamename=new String("hello")//console mai object hai string   
console.log(gamename.length)
const newstringg=gamename.substring(0,4)
const anotherstring =gamename.slice(-2,4)//can be used using the negative avlues and it reads form the last then
console.log(anotherstring)
const trimex="  helo   ";
console.log(trimex.trim())// trim removes the blank spaces from start and end of the string 


